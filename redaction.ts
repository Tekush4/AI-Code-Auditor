const SECRET_PATTERNS=[/AKIA[0-9A-Z]{16}/g,/sk_live_[A-Za-z0-9]{16,}/g,/gh[pousr]_[A-Za-z0-9]{36,}/g,/xox[baprs]-[0-9A-Za-z-]{10,}/g,/-----BEGIN [A-Z ]*PRIVATE KEY-----[\s\S]*?-----END [A-Z ]*PRIVATE KEY-----/g,/((?:postgres|postgresql|mysql|mongodb):\/\/[^:\s]+:)([^@\s]+)(@)/gi];
export function redactSensitiveData(text:string):string{let out=text;for(const r of SECRET_PATTERNS)out=out.replace(r,(...args)=>{const m=args[0] as string;if(r.source.includes('postgres'))return `${args[1]}[REDACTED]${args[3]}`;return `[REDACTED_SECRET]`;});return out.replace(/(password|passwd|token|api[_-]?key|secret)\s*[:=]\s*["'][^"']+["']/gi,"$1: \"[REDACTED_SECRET]\"");}
