import {test} from "node:test";import assert from "node:assert/strict";import {mkdtempSync,writeFileSync,mkdirSync,rmSync} from "node:fs";import {tmpdir} from "node:os";import path from "node:path";import {AnalysisPipeline} from "../../backend/src/modules/analysis/pipeline.ts";
test("SEC-PIPELINE-001: pipeline completo produz resultado único com engines e relatório",async()=>{const dir=mkdtempSync(path.join(tmpdir(),"aica-pipeline-"));mkdirSync(path.join(dir,"src"));writeFileSync(path.join(dir,"package.json"),JSON.stringify({dependencies:{lodash:"4.17.20"}}));writeFileSync(path.join(dir,"src","bad.ts"),`const x = process.argv[2]; eval(x);`);const r=await new AnalysisPipeline().run("p1","demo",dir);assert.equal(r.status,"COMPLETED");assert.ok(r.languages.some(x=>x.language==="javascript"||x.language==="typescript"));assert.ok(r.findings.some(f=>f.category==="Dangerous Functions"));assert.ok(r.findings.some(f=>f.category==="Vulnerable Dependency"));assert.equal(typeof r.risk.overallScore,"number");assert.ok(r.reports.json.includes("demo"));assert.ok(r.reports.markdown.includes("Findings"));rmSync(dir,{recursive:true,force:true});});
