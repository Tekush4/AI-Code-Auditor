import {test} from "node:test";import assert from "node:assert/strict";import {openDatabase} from "../../backend/src/modules/db/database.ts";import {UserRepository,SessionRepository} from "../../backend/src/modules/auth/users.ts";import {AnalysisRepository} from "../../backend/src/modules/analysis/repository.ts";
test("SEC-ANALYSIS-AUTHZ-001: usuário B não acessa análise de usuário A",async()=>{const db=openDatabase(":memory:");const users=new UserRepository(db),sessions=new SessionRepository(db),repo=new AnalysisRepository(db);const a=await users.register("usera","senha-segura-123"),b=await users.register("userb","senha-segura-456");const now=new Date().toISOString();const result={id:"analysis-a",projectId:"project-a",projectName:"a",status:"COMPLETED",startedAt:now,finishedAt:now,duration:1,import:{files:[],rejected:[],truncated:false,totalBytes:0},languages:[],architecture:{languages:[],frameworks:[],modules:[],dependencies:[],components:[],relationships:[],cycles:[],risks:[],limitations:[]},performance:{findings:[],limitations:[]},ai:{findings:[],summary:"",limitations:[]},findings:[],risk:{overallScore:0,totalFindings:0,bySeverity:{CRITICAL:0,HIGH:0,MEDIUM:0,LOW:0,INFO:0},scores:[]},reports:{json:"{}",markdown:""},limitations:[]} as any;repo.save(a,result);assert.equal(repo.get("analysis-a",b),null);assert.equal(repo.findings("analysis-a",b),null);assert.equal(repo.report("analysis-a",b),null);sessions.create(a);});
